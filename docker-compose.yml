version: '3.8'

services:
  study_react_service7:
    container_name: study_react_container
    build:
      context: ""
    volumes:
      - ./frontend7:/usr/src/app
    ports:
      - '3000:3000'
    stdin_open: true
    tty: true