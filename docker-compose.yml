version: '3.8'

services:
  study_react_service6:
    container_name: study_react_container
    build:
      context: ""
    volumes:
      - ./frontend6:/usr/src/app
    ports:
      - '3000:3000'
    stdin_open: true
    tty: true